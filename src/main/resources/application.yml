spring:
  datasource:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    url: jdbc:hsqldb:file:db\itinfodb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update

google:
  api:
    host: www.googleapis.com
    path: books/v1/volumes

logging:
  level:
    root: info
    ru:
      home:
        itinfo: debug
